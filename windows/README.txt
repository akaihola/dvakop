The Windows keyboard layout in this directory doesn't (yet) change the layout of
Ctrl combinations.

Also, Ctrl and Caps Lock keys need to be swapped separately using the
`swap-ctrl-caps.ps1` PowerShell script.
